export enum ColumnType {
  Text = 'text',
  Date = 'date',
  Datetime = 'datetime',
  Integer = 'integer',
  Decimal = 'decimal',
  Currency = 'currency',
  Boolean = 'boolean',
  Percent = 'percent',
  Enum = 'enum',
  Lookup = 'lookup'
}