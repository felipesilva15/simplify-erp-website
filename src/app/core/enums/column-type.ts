export enum ColumnType {
  TEXT = 'text',
  DATE = 'date',
  DATETIME = 'datetime',
  INTEGER = 'integer',
  DECIMAL = 'decimal',
  CURRENCY = 'currency',
  BOOLEAN = 'boolean',
  PERCENT = 'percent'
}